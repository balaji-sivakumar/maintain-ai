# Model provider: "openai" (default, Stage A) or "bedrock" (Stage B / Option A)
MODEL_PROVIDER=openai

# Required when MODEL_PROVIDER=openai
OPENAI_API_KEY=

# Required when MODEL_PROVIDER=bedrock (uses standard AWS credential chain otherwise)
AWS_REGION=us-east-1

# Neon Postgres connection string (Day 5 deployment; get this from neon.tech project dashboard)
DATABASE_URL=

# Chroma Cloud (get these from trychroma.com dashboard). Leave all three
# blank to fall back to a local PersistentClient (writes to data/chroma/)
# for local dev without a Chroma Cloud account.
CHROMA_API_KEY=
CHROMA_TENANT=
CHROMA_DATABASE=

# Resend (get an API key at resend.com). Leave both blank to fall back to a
# ConsoleNotifier that just prints to stdout, for local dev without a Resend
# account. The sandbox sender (onboarding@resend.dev) can only deliver to
# NOTIFY_EMAIL — your Resend account's own signup email.
RESEND_API_KEY=
NOTIFY_EMAIL=

# OTel tracing export (get an API key at honeycomb.io). Strands already emits
# spans for every chat turn/tool call internally; this just gives them a
# destination. Leave blank to stay a no-op (no verbose span JSON locally).
OTEL_EXPORTER_OTLP_ENDPOINT=https://api.honeycomb.io/
OTEL_EXPORTER_OTLP_HEADERS=x-honeycomb-team=
OTEL_SERVICE_NAME=maintain-ai
