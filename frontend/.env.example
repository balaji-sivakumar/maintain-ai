# Base URL of the deployed Maintain-AI backend (Railway web service).
# The WebSocket URL for the live trace is derived from this (http -> ws).
NEXT_PUBLIC_API_BASE=https://web-production-91b0a.up.railway.app
