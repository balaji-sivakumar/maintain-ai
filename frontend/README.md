# Maintain-AI — Frontend

Placeholder for the live tool-trace judging UI: a Next.js app deployed on Vercel that subscribes to the backend's WebSocket event stream and renders the orchestrator's tool calls as they execute.

Not yet scaffolded — see [`ARCHITECTURE.md`](../ARCHITECTURE.md#live-tool-trace-judging-ui) for the design and [`MILESTONES.md`](../MILESTONES.md) for when it's built (Day 3 stretch: expose the WebSocket endpoint; Day 5 stretch: build and deploy this app). It's demo polish, not a submission requirement, so it stays empty until the backend's core agent loop works.
